Stop asking for HyperPay secrets. That is not the issue right now.

Current production issue:
- Mobile login on quranesh.com shows only "Login failed"
- Scholarship flow on exercise pages still shows invalid session on mobile

From the current production logs, I do not yet see the exact auth failure reason.
So now focus ONLY on live auth debugging.

Do this exactly:

1) Ignore HyperPay for now.
Do not block auth debugging on missing payment secrets.

2) Add explicit production logs for these endpoints:
- POST /api/login
- POST /api/signup
- POST /api/scholarship/claim

For each request log:
- route name
- email if present
- whether request body parsed correctly
- whether user was found
- whether password check passed
- whether JWT was generated
- whether response returned 200 / 401 / 500
- full error object fields:
  name, message, code, detail, constraint, stack

3) In client/src/pages/SignIn.tsx
- stop showing only generic "Login failed"
- during debugging, surface the exact backend reason in console
- map user-facing text to Arabic:
  - invalid credentials -> البريد الإلكتروني أو كلمة المرور غير صحيحة
  - server error -> تعذر تسجيل الدخول حاليا، حاول مرة أخرى
  - session expired -> انتهت الجلسة، يرجى تسجيل الدخول مرة أخرى

4) In client/src/hooks/use-auth.tsx
- after login success, log:
  - token received?
  - token saved to localStorage?
  - user saved?
  - auth state updated?
- on page reload, log:
  - token exists?
  - token decodes?
  - token expired?
- if malformed/expired, clear auth

5) In client/src/components/subscription-gate.tsx
- before scholarship claim, log whether Authorization token exists
- if missing, redirect cleanly to signin
- never show raw English invalid session text

6) Test on the LIVE domain quranesh.com after republish:
A. Login with a known valid non-admin user
B. Login with admin user
C. Signup a brand new user
D. Claim scholarship from mobile after login

For each test report:
- exact request URL
- exact status code
- exact JSON response
- exact redirect behavior
- exact server log lines

Important:
Do not answer "works locally".
I need the exact production response body for /api/login and /api/scholarship/claim on quranesh.com.